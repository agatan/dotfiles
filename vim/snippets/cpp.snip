# class { ... };
snippet     class
abbr        class {}
options     head
    class ${1:#:name} ${2}{
    public:
      $1() = default;
      $1($1 const&) = default;
      $1($1 &&) = default;
      $1 &operator=($1 const&) = default;
      $1 &operator=($1 &&) = default;

      ~$1() = default;

    private:
    };
